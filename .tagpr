[tagpr]
    # Release branch
    releaseBranch = main
    
    # Version file - use git tags only (no version file)
    versionFile = -
    
    # Add v prefix to tags (e.g. v0.2.0)
    vPrefix = true
    
    # Update changelog
    changelog = true
    
    # Create GitHub releases
    release = true
    
